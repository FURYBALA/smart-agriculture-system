# Wokwi simulation config for the irrigation node. See
# ../../../docs/wokwi-simulation.md for real, actually-attempted
# results -- do not assume this passes.
[wokwi]
version = 1
firmware = "../build/irrigation_node.ino.bin"
elf = "../build/irrigation_node.ino.elf"
