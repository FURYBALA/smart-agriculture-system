# Wokwi simulation config for the irrigation node. Actually run and
# passing as of this config -- see ../../../docs/wokwi-simulation.md
# for the real, actually-attempted results and how this was verified.
[wokwi]
version = 1
firmware = "../build/irrigation_node.ino.bin"
elf = "../build/irrigation_node.ino.elf"
