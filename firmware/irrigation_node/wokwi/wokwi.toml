# Wokwi simulation config for the irrigation node. NOT executed as part
# of this repo's verification -- see ../../../docs/wokwi-simulation.md
# for exactly what that means and what it would take to run it.
[wokwi]
version = 1
firmware = "../build/irrigation_node.ino.bin"
elf = "../build/irrigation_node.ino.elf"
