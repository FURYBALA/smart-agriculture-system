name: Firmware compile check

# Neither sketch has ever been flashed to real hardware -- there's no
# ESP32 board available in this project's dev environment. This is
# the next best thing: actually compile both against real ESP32 board
# definitions and libraries on every push, so a broken include or API
# mismatch is caught here instead of at someone's first real flash.

on:
  push:
    paths:
      - "firmware/**"
      - ".github/workflows/firmware-compile.yml"
  pull_request:
    paths:
      - "firmware/**"
  workflow_dispatch:

jobs:
  irrigation-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 board core
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install libraries
        run: |
          arduino-cli lib install "DHT sensor library"
          arduino-cli lib install "Adafruit Unified Sensor"
          arduino-cli lib install "ArduinoJson"

      - name: Compile irrigation_node
        run: |
          arduino-cli compile --fqbn esp32:esp32:esp32 firmware/irrigation_node

  vision-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 board core
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install TFLite Micro library
        run: arduino-cli lib install "Chirale_TensorFlowLite"

      - name: Compile vision_node
        run: |
          arduino-cli compile --fqbn esp32:esp32:esp32cam firmware/vision_node

  host-logic-tests:
    # Compiles and runs firmware/test/*.cpp against the hardware-independent
    # logic headers (irrigation_logic.h, vision_logic.h) with the runner's
    # plain g++ -- no ESP32 board, no Arduino toolchain. This is real
    # execution of real production logic (pump state machine, thresholds,
    # timer-origin tracking, RGB565 decode, INT8 quantize/dequantize,
    # argmax), not a compile-only check. See docs/host-testing.md.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build and run irrigation_logic host test
        run: |
          g++ -std=c++17 -Wall -Wextra -I firmware/irrigation_node \
            firmware/test/test_irrigation_logic.cpp -o test_irrigation_logic
          ./test_irrigation_logic

      - name: Build and run vision_logic host test
        run: |
          g++ -std=c++17 -Wall -Wextra -I firmware/vision_node \
            firmware/test/test_vision_logic.cpp -o test_vision_logic
          ./test_vision_logic
